requests>=2.31
# Imita la huella TLS de Chrome. Sin esto, Cloudflare devuelve 403 a todo.
curl_cffi>=0.7
# Base de datos IANA de zonas horarias. Linux la trae, Windows no, y sin ella
# zoneinfo no resuelve "America/Santiago".
tzdata>=2024.1
